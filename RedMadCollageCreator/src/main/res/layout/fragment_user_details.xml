<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
              android:layout_width="match_parent"
              android:layout_height="match_parent"
              android:orientation="vertical"
              android:paddingLeft="@dimen/defHWindowPadding"
              android:paddingTop="@dimen/defVWindowPadding"
              android:paddingRight="@dimen/defHWindowPadding"
              android:paddingBottom="@dimen/defVWindowPadding">

    <LinearLayout android:layout_width="match_parent"
                  android:layout_height="wrap_content"
                  android:orientation="horizontal">
        <LinearLayout android:layout_width="0dp"
                      android:layout_height="wrap_content"
                      android:layout_weight="1"
                      android:orientation="vertical">
            <TextView android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:layout_marginBottom="@dimen/defVMargin"
                      android:textSize="18sp"
                      android:textStyle="bold"
                      android:textColor="@color/abc_search_url_text_holo"
                      android:text="ivangusef"
                      android:id="@+id/user_name"/>
            <TextView android:layout_width="wrap_content"
                      android:layout_height="wrap_content"
                      android:textSize="18sp"
                      android:text="fullname"
                      android:id="@+id/full_name"/>
        </LinearLayout>
        <ImageView android:layout_width="@dimen/thumb_size"
                   android:layout_height="@dimen/thumb_size"
                   android:scaleType="centerCrop"
                   android:contentDescription="@string/user_photo"
                   android:background="@drawable/ic_anonymoususer"
                   android:id="@+id/profile_picture"/>

    </LinearLayout>

</LinearLayout>